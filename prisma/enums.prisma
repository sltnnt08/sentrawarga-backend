enum Role {
    USER
    ADMIN
}

enum Priority {
    LOW
    NORMAL
    HIGH
    CRITICAL
}

enum Category {
    CRIMINAL
    TRASH
    FLOOD
    POLLUTION
    ROADS_ISSUE
    PUBLIC_DISTURBANCE
    ACCIDENTS
    OTHERS
}

enum ReportStatus {
    PENDING
    VERIFIED
    IN_PROGRESS
    RESOLVED
    REJECTED
    CANCELLED
}

enum VoteType {
    UP
    DOWN
}

enum NotificationType {
    REPORT_STATUS_CHANGED
    NEW_COMMENT
    REPORT_RESOLVED
    ADMIN_REPLY
}
